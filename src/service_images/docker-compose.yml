# This file is for testing purposes only

version: '3'
services:
  django:
    build:
      context: ./django
    volumes:
      -  "c:/Users/Cailan/Documents/SENG_499/cumulus_test_workspace/django:/cumulus/django"
    ports:
      - "8081:8080"
    environment:
      CUMULUS_MODE: START
  mysql:
    build:
      context: ./mysql
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      CUMULUS_MODE: START
    ports:
      - "3306"
    volumes:
      -  "c:/Users/Cailan/Documents/SENG_499/cumulus_test_workspace/mysql:/cumulus/mysql"
      - "mysql-data:/var/lib/mysql"
volumes:
  mysql-data:
